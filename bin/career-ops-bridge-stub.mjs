#!/usr/bin/env node

/**
 * career-ops-bridge -- placeholder for the hosted-webapp local bridge.
 *
 * Phase-2 deliverable per
 * /home/fullstackfang/.claude/plans/is-there-a-way-generic-walrus.md (channel E).
 * The bridge is *not* implemented in v1; this stub exists to reserve the
 * `career-ops bridge` subcommand and prevent accidental shipping of stale code
 * under that name.
 */

console.error(`career-ops bridge: not implemented yet.

This subcommand will eventually pair your local machine with a hosted
career-ops web UI so you can use the system from any device while the
agent still runs under your local Claude Code subscription.

Tracked as: distribution channel E in
docs/superpowers/specs/2026-05-03-shareable-app-design.md.`);

process.exit(2);
